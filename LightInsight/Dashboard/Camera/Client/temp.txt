using System;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Controls.Primitives; // Thêm thư viện này cho Thumb
using System.Windows.Media;

namespace LightInsight.Dashboard.Camera.Client
{
	public partial class CameraOfflineDurationTop10 : UserControl
	{
		public event EventHandler DeleteRequested;

		public CameraOfflineDurationTop10()
		{
			InitializeComponent();
			DeleteButton.Visibility = Visibility.Collapsed;

			// 1. Logic mặc định chiếm 1/2 chiều dài khung chứa khi load
			this.Loaded += (s, e) =>
			{
				var parent = VisualTreeHelper.GetParent(this) as FrameworkElement;
				if (parent != null)
				{
					this.Width = parent.ActualWidth / 2;
				}
			};
		}

		public void SetEditMode(bool isEdit)
		{
			DeleteButton.Visibility = isEdit ? Visibility.Visible : Visibility.Collapsed;
			// Hiện điểm kéo khi ở chế độ edit (tùy chọn)
			ResizeThumb.Visibility = isEdit ? Visibility.Visible : Visibility.Collapsed;
		}

		private void DeleteWidget_Click(object sender, RoutedEventArgs e)
		{
			DeleteRequested?.Invoke(this, EventArgs.Empty);
		}

		// 2. Logic kéo dãn kích thước
		private void ResizeThumb_DragDelta(object sender, DragDeltaEventArgs e)
		{
			// Thay đổi Width/Height dựa trên khoảng cách di chuyển của chuột
			double newWidth = this.ActualWidth + e.HorizontalChange;
			double newHeight = this.ActualHeight + e.VerticalChange;

			if (newWidth >= this.MinWidth) this.Width = newWidth;
			if (newHeight >= this.MinHeight) this.Height = newHeight;
		}
	}
}


<UserControl x:Class="LightInsight.Dashboard.Camera.Client.CameraOfflineDurationTop10"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="150"
             MinHeight="100" MinWidth="150">
	<Border Background="#2D2D30"
            BorderThickness="1"
            CornerRadius="6"
            Padding="0">
		<Border.BorderBrush>
			<VisualBrush>
				<VisualBrush.Visual>
					<Rectangle Stroke="#E8751A"
                               StrokeDashArray="4 2"
                               StrokeThickness="1"
                               RadiusX="6" RadiusY="6"
                               Width="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualWidth}"
                               Height="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualHeight}"/>
				</VisualBrush.Visual>
			</VisualBrush>
		</Border.BorderBrush>

		<Grid>
			<Border Background="#3E3E42"
                    BorderBrush="#33FFFFFF"
                    BorderThickness="0,0,0,1"
                    Height="25"
                    VerticalAlignment="Top"
                    Padding="10,0,0,0"
                    CornerRadius="6,6,0,0">
				<TextBlock Text="Camera Status Donut"
                           Foreground="White"
                           FontSize="14"
                           VerticalAlignment="Center"/>
			</Border>

			<Button x:Name="DeleteButton"
                    Content="X"
                    Width="30" Height="23"
                    MinWidth="0" MinHeight="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,2,2,0"
                    FontSize="16"
                    Click="DeleteWidget_Click"/>

			<Thumb x:Name="ResizeThumb" 
                   Width="10" Height="10" 
                   Cursor="SizeNWSE" 
                   VerticalAlignment="Bottom" 
                   HorizontalAlignment="Right"
                   DragDelta="ResizeThumb_DragDelta">
				<Thumb.Template>
					<ControlTemplate>
						<Grid Background="Transparent">
							<Path Data="M 8,0 L 8,8 L 0,8 Z" Fill="#55FFFFFF" Margin="0,0,1,1" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
						</Grid>
					</ControlTemplate>
				</Thumb.Template>
			</Thumb>
		</Grid>
	</Border>
</UserControl>