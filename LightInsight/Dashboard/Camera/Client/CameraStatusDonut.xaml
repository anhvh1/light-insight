<UserControl x:Class="LightInsight.Dashboard.Camera.Client.CameraStatusDonut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="130"
             Width="250">

	<Border Background="#2D2D30"
            
            BorderThickness="1"
            CornerRadius="6"
            Padding="0">
		<Border.BorderBrush>
			<VisualBrush>
				<VisualBrush.Visual>
					<Rectangle Stroke="#E8751A" 
                           StrokeDashArray="4 2" 
                           StrokeThickness="1"
                           Width="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualWidth}"
                           Height="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=ActualHeight}"/>
				</VisualBrush.Visual>
			</VisualBrush>
		</Border.BorderBrush>
		<Grid>
			<Border Background="#3E3E42" 
                    BorderBrush="#33FFFFFF" 
                    BorderThickness="0,0,0,1" 
                    Height="25"
                    VerticalAlignment="Top"
                    Padding="10,0,0,0"
                    CornerRadius="6"
                    >
				<TextBlock Text="Camera Status Donut"
                           Foreground="White"
                           FontSize="11"
                           VerticalAlignment="Center"/>
			</Border>

			<Button x:Name="DeleteButton"
                Content="X"
                Width="15" 
                Height="15"
                MinWidth="0"   
                MinHeight="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                HorizontalContentAlignment="Center" 
                VerticalContentAlignment="Center"
                Padding="0" 
                FontSize="10"
                Click="DeleteWidget_Click"/>


			<Grid Margin="0,25,0,0">
				<Ellipse x:Name="BackgroundCircle"
                 Stroke="#F44336" 
                 StrokeThickness="12" 
                 Width="70" Height="70"/>

				<Ellipse x:Name="OnlineSlice"
                     Stroke="#2ECC71" 
                     StrokeThickness="12" 
                     Width="70" Height="70"
                     StrokeDashArray="0, 100"  
                     RenderTransformOrigin="0.5,0.5">
					<Ellipse.RenderTransform>
						<TransformGroup>
							<ScaleTransform ScaleX="1" ScaleY="-1"/>
							<RotateTransform Angle="0"/>
						</TransformGroup>
					</Ellipse.RenderTransform>
				</Ellipse>

				<StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0">
					<TextBlock Text="Online: " Foreground="#2ECC71" FontSize="12"/>
					<TextBlock x:Name="TxtOnline" Text="0" Foreground="#2ECC71" FontSize="12" FontWeight="Bold"/>
				</StackPanel>

				<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0">
					<TextBlock x:Name="LblOffline" Text="Offline: " Foreground="#F44336" FontSize="12"/>
					<TextBlock x:Name="TxtOffline" Text="0" Foreground="#F44336" FontSize="12" FontWeight="Bold"/>
				</StackPanel>
			</Grid>
		</Grid>
	</Border>
</UserControl>